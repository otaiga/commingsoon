<div class="container">
  <div class="row">
    <div class="span8 offset2">
      <% @response.each_pair do |date, movie| %>
        <h3 class="smallTitle">Available on&nbsp;<%= date %></h3>
        <% movie.each do |m| %>
          <div class="thumbnail">
            <div class="caption">
              <%= link_to m.name, "http://www.imdb.com/title/" + m.title_id, :class => 'smallTitle fontSize13' %><br>
              <p class="smallTitle">Bio</p><br>
              <p class="bio"><%= m.bio %></p>
              <p class="smallTitle">Cast</p><br>
              <p class="bio"><%= m.cast.join(", ") %></p>
              <%= link_to "remind me", "#", :class => 'button' %>
            </div>
            <img src=<% if m.image_link %> <%= m.image_link %> width="180" height="250"<% else %> "http://placehold.it/180x250" <% end %>>
          </div>
        <% end %>
      <% end %>
    </div><!--span8 offset2-->
  </div><!--Row-->
</div><!--/container-->
